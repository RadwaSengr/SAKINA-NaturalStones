# QA Notes

- The `/collection` route returns all six website-owned stones with the approved EGP prices: 300, 380, 460, 560, 680, and 800.
- The first viewport may show lazy image placeholders in an immediate full-page capture; after the collection is in view, the individual-stone images load correctly.
- The customer-facing collection copy and order-review links state that delivery details and confirmation stay inside SAKINA.
- A browser-index click on the first visible “Add to bag” control did not create the expected `sakina:local-cart` entry, so the interactive collection-to-bag flow requires a focused runtime check before release.
- A controlled DOM click produced the same initial storage observation and no browser-console error. The next check will inspect the rendered bag state and the selected control rather than infer the result from console evaluation alone.
- The subsequent rendered state confirmed that the local bag contains Clear Quartz at 300 EGP and routes directly to `/checkout`. The order review shows 118 EGP delivery, a 418 EGP single-stone total, COD, the disabled card preview, and the explicit in-site purchase assurance.
- Selecting the tasbih radio directly updates the in-site breakdown to 500 EGP for one stone and an order total of 918 EGP. Increasing Clear Quartz to two stones updates the product subtotal to 600 EGP, the tasbih fee to 550 EGP, and the total to 1,268 EGP, including the 118 EGP delivery fee.
- In Arabic, the same two-stone single-piece state renders 600 EGP for products, 118 EGP for delivery, and a 718 EGP total with Arabic labels and numerals. The tasbih option and its 500 EGP plus 50 EGP-per-additional-stone formula remain plainly visible.
- In Arabic tasbih mode for two Clear Quartz stones, the checkout visibly shows 550 EGP for “تجهيز السبحة والخرز” and a 1,268 EGP final total, calculated from 600 EGP for products, 118 EGP delivery, and the assembly fee.

Owner-only privacy QA: the authenticated owner mobile preview shows the in-app SAKINA management entry in the compact header and the protected `/owner?mobile=1` workspace opens with the owner order dashboard, private-access label, return-home action, and status cards. The customer storefront keeps the public navigation and does not expose owner order content. Automated access coverage also confirms an unauthenticated visitor is redirected to `/` without rendered owner content, while a different admin identity is rejected by the server owner procedure.
Post-checkpoint phone QA at 375x812: the authenticated owner storefront shows the compact in-app «إدارة» control in the header and as a fifth bottom-navigation item; `/owner?mobile=1` opens the private owner dashboard with order counts and delivery/order data. The desktop post-checkpoint preview also shows the «إدارة» header control and owner workspace.
Owner sign-in recovery QA: the current dev preview at `/owner` loads the authenticated owner order workspace for the persisted owner session. The published domain was observed on the pre-recovery version during this check and should be refreshed by the new checkpoint; the recovery UI is implemented in the current project and covered by access tests.

Owner swipe-access QA: at a 375x812 phone viewport, the default taskbar contains only the four public destinations (home, cabinet, passport, bag), with no visible owner-management item and no owner shortcut in the compact header. Automated touch tests confirm an intentional upward swipe opens the private access panel for the owner, while a previously verified owner phone can reopen a sign-in action after logout without displaying order data. The desktop owner shortcut remains protected by owner identity.

Corrected swipe-access QA: the phone viewport again shows only the four public tabs before any gesture, and the desktop layout no longer renders the mobile swipe panel. The touch test now verifies that a deliberate upward swipe opens a sign-in-only action even after logout on a phone with no stored owner hint; no order information is included in that panel.
Live mobile swipe verification: a 375x812 touch-enabled Chromium session dispatched a real upward swipe from the bottom app bar. The panel then reported `aria-hidden=false`, computed `display:grid`, and `opacity:1`; it contained only the generic private-access/sign-in copy, while the owner dashboard was not rendered.
Owner orders-header QA: the authenticated 375x812 mobile preview visibly renders an «الطلبات» entry with the dashboard icon above the storefront content, while the main taskbar remains separate. The entry has a protected `/owner` destination; a live owner-session click opened the independent orders workspace, and customer/header tests confirm that the link is absent without owner identity.
Desktop owner-orders QA: the latest 1280x720 owner preview keeps the protected management entry in the storefront header and opens the independent `/owner` workspace. The dedicated orders page renders its status filters and owner-only layout correctly, without exposing it through the public taskbar.

Published update diagnosis: the published domain served an older anonymous storefront response at the bare URL, while a cache-busting query served the current app shell. The published `/owner` route confirmed the current owner sign-in gate, and the browser reported no Service Worker registrations. The server static delivery now sends `no-store`/`no-cache` for `index.html` so installed mobile launches fetch the current application shell instead of retaining an older HTML entry point.

Published release verification after cache fix: opening the deployed `/owner?release=4e3c843c` route on the published domain now returns the current SAKINA owner sign-in gate with the message that order data is hidden until account verification. An unauthenticated published storefront response contains no owner-management entry, as expected; authenticated owner-only entry behavior remains covered by the 30 passing client/server tests and was verified in the mobile and desktop preview sessions.

Mobile orders debugging: the signed-in owner route is live and displays the separate order workspace and order counts. The responsive preview showed the owner page can still be rendered through the dashboard shell at a desktop CSS width in some browser emulation contexts, so the next fix adds a CSS media-query-controlled «الطلبات» shortcut inside the private owner page itself, independent of the JS mobile-width detector.

Published owner verification — 27 Aug 2026: the live `auth.me` response on `[deployed preview]` identifies the authenticated SAKINA owner as `isOwner: true`. In the same authenticated production session, Chromium device emulation at 390×844 confirmed that the visible header label is «الطلبات» (the desktop label is hidden), its destination is `/owner`, and the four-item public taskbar remains unchanged. Activating the live header control opened the published `/owner` page at the same phone viewport with the «طلبات المتجر» heading, private-owner label, order status counts, and six order cards. The production server test suite also rejects a customer and a second admin identity from the owner APIs.

Owner acceptance confirmation — 27 Aug 2026: the owner confirmed that the published-link check is correct. The verified mobile flow is: sign in with the owner account, use the visible header entry «الطلبات», then open the protected `/owner` order workspace. The public bottom taskbar remains limited to the four storefront destinations.

Published sign-in round-trip confirmation — 27 Aug 2026: after being asked to test the exact production link, the owner confirmed that the owner sign-in and resulting orders screen were correct. This confirms the real-device acceptance flow from the published `/owner` access point back to the protected orders workspace.

Browser-observed published OAuth round-trip — 27 Aug 2026: pre-login state was `[deployed preview]/owner`, showing only the «دخول المالكة» gate and no order data. The generated sign-in request carried the safe `returnTo: "/owner"` state. After selecting the authenticated owner account, the browser reached the callback on the same production host and then loaded `[deployed preview]/owner`, where «طلبات المتجر» rendered with the owner-only access label, six order cards, and status counts. This is a direct published-domain round-trip capture, not a preview result.

Published notification and Excel verification — 27 Aug 2026: the authenticated production owner page at `[deployed preview]/owner` visibly renders the «تصدير Excel» control with the six current orders. After the Excel module completed its background load, activating the control generated a browser download link named `sakina-orders-2026-08-27.xlsx` with a Blob-backed `.xlsx` payload and no export error. The server order tests confirm that a notification is requested only after a COD order is stored, that it contains operational order data rather than the customer address or contact details, and that a temporary notification failure never blocks checkout.

Published date-filter verification — 27 Aug 2026: the authenticated owner page on `[deployed preview]/owner` renders the Arabic «تصفية بالتاريخ» panel with start, end, and reset controls. Selecting 26 Aug 2026 as both boundaries reduced the visible list and status counts from six to two matching orders; triggering Excel export then created the expected Blob download while the screen reported «2 طلباً مطابقاً». Reversing the boundaries displayed the explicit Arabic validation message and zero results; «مسح الفترة» restored all six orders. Automated coverage verifies inclusive first/last-day boundaries and invalid-range handling.
